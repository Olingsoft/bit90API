# Server
PORT=3000
# Bind host — use 0.0.0.0 for Deno Deploy / cloud runtimes.
# Leave unset locally; defaults to 0.0.0.0 automatically.
# HOST=0.0.0.0

# MongoDB
MONGODB_URI=mongodb://127.0.0.1:27017/vvv-api

# Authentication
JWT_SECRET=change-me-in-production

# Admin signup (optional — disables admin signup when unset)
ADMIN_SIGNUP_SECRET=

# One-time Firestore migration only (scripts/migrate-firestore-to-mongo.js)
# FIREBASE_API_KEY=
# FIREBASE_AUTH_DOMAIN=
# FIREBASE_PROJECT_ID=
# FIREBASE_STORAGE_BUCKET=
# FIREBASE_MESSAGING_SENDER_ID=
# FIREBASE_APP_ID=
